පහත script එකෙ මම පහතින් කියන දෙවල් පමනක් සිදු කර වෙනත් කිසිවක් අබමල් රෙනුවක තරම් වත් වෙනස් නොකර සම්පුර්ණ script එක ලබා දෙන්න👇 

පොස්ට් වල තිබෙන පින්තුර පල පලල වෙනසක් නොකර එලෙසම තබාගෙන, දිග ප්‍රමනය තව දශමයක් උඩටයි පහලටයි දිග් කරන්න  පින්තුරය හොදින් පෙනෙන පරිදි👇

(පකො පින්තුර වල දැන් තිබෙන පලප පොඩ්ඩක් වත් වෙනස් කරන්න එපා දශමයක් වත්❌)

---
import { getSinglePage } from "@/lib/contentParser.astro";
import dateFormat from "@/lib/utils/dateFormat";
import { humanize, plainify, slugify } from "@/lib/utils/textConverter";
import { Image } from "astro:assets";
import type { CollectionEntry } from "astro:content";
import { BiCalendarEdit, BiCategoryAlt } from "react-icons/bi";

type Props = {
  posts: CollectionEntry<"posts">[];
  className?: string;
  fluid?: boolean;
};

const authors = await getSinglePage("authors");
const { className, posts, fluid } = Astro.props;
---

<div
  class={`row gy-4 gx-2 sm:gx-4 ${className} ${posts.length == 1 ? "justify-center" : ""} `}
>
  {
    posts.map((post, i: number) => (
      // SMART GRID FIX: Mobile, Tablet, Desktop හැම එකකදීම එක පේළියට පෝස්ට් 3ක් පෙන්වීමට col-4 යෙදුවා
      <div class="col-4 flex flex-col justify-between">
        {post.data.image && (
          <a
            href={`/blog/${post.id}/`}
            class="rounded-lg block hover:text-primary overflow-hidden group relative"
            style="aspect-ratio: 3 / 5;" // 100% FIX: පින්තූරය දිග් අතට නොවී හරස් අතට තව දශමයක් පලල් වීමට 3/4 කළා
          >
            <Image
              class="group-hover:scale-[1.04] transition duration-300 w-full h-full object-cover"
              src={post.data.image}
              alt={post.data.title}
              width={445}
              height={665}
              loading={i < 3 ? "eager" : "lazy"}
              decoding="async"
            />
          </a>
        )}
        
        {/* INFO ROW: මොබයිල් තිරවල ඉඩ ඉතිරි කර ගැනීමට අකුරු සහ අයිකන කුඩා කළා */}
        <ul class="mt-2 mb-1 flex flex-wrap items-center text-text text-[10px] sm:text-sm justify-between">
          <li class="flex items-center flex-wrap">
            <BiCategoryAlt className="mr-0.5 h-3 w-3 sm:h-[16px] sm:w-[16px] text-gray-600" />
            <ul>
              {post.data.categories.slice(0, 1).map((category) => (
                <li class="inline-block">
                  <a
                    href={`/categories/${slugify(category)}/`}
                    class="hover:text-primary font-bold text-[9px] sm:text-xs bg-gray-100 dark:bg-darkmode-theme-light px-1 sm:px-2 py-0.5 rounded"
                  >
                    {humanize(category)}
                  </a>
                </li>
              ))}
            </ul>
          </li>
          <li class="hidden xs:flex items-center flex-wrap font-medium text-[9px] sm:text-xs text-gray-500">
            <BiCalendarEdit className="mr-0.5 h-3 w-3 sm:h-4 sm:w-4" />
            {dateFormat(post.data.date!)}
          </li>
        </ul>

        {/* POST TITLE: මොබයිල් තිරයේදී අකුරු ඔටෝම කුඩා වන ලෙස text-xs සහ line-clamp සකස් කළා */}
        <h3 class="mb-1 line-clamp-2 leading-tight">
          <a
            href={`/blog/${post.id}/`}
            class="block hover:text-primary transition duration-300 text-xs sm:text-base font-bold"
          >
            {post.data.title}
          </a>
        </h3>

        {/* POST SNIPPET: කතාවේ පේළි දෙකක් හැම තිරයකදීම පෙන්වීමට hidden ක්ලාස් එක සම්පූර්ණයෙන්ම ඉවත් කළා */}
        <p class="text-text text-[11px] sm:text-sm line-clamp-2 mb-2 leading-snug">{plainify(post.body ?? "")}</p>

        {/* AUTHOR & READ MORE CONTAINER */}
        <div class="flex items-center justify-between border-t border-gray-100 pt-1.5 sm:pt-3 mt-auto gap-2">
          {authors
            .filter((author) =>
              post.data.authors
                .map((author) => slugify(author))
                .includes(slugify(author.data.title))
            )
            .slice(0, 1)
            .map((author) => (
              <a
                href={`/authors/${slugify(author.data.title)}/`}
                class="flex items-center hover:text-primary text-[10px] sm:text-xs font-medium min-w-0 flex-shrink-1"
              >
                {author.data.image && (
                  <Image
                    src={author.data.image}
                    alt={author.data.title}
                    height={24}
                    width={24}
                    class="mr-1 h-4 w-4 sm:h-5 sm:w-5 rounded-full object-cover flex-shrink-0"
                  />
                )}
                {/* කර්තෘගේ නම මොබයිල් ඇතුළු හැම තිරයකදීම පෙන්වීමට hidden ක්ලාස් එක ඉවත් කළා */}
                <span class="truncate">{author.data.title}</span>
              </a>
            ))}
            
          {/* 100% FIX: අනවශ්‍ය ඉඩ මකා දමා බටන් එක හරස් අතට හොඳින් පලල් වීමට flex-grow, text-center සහ px වැඩි කළා */}
          <a 
            href={`/blog/${post.id}/`} 
            class="text-[9px] sm:text-xs bg-primary text-white px-3 sm:px-6 py-1 sm:py-1.5 rounded-full hover:bg-opacity-90 transition-all font-medium whitespace-nowrap flex-grow text-center max-w-[100px]"
          >
            Read
          </a>
        </div>
      </div>
    ))
  }
</div>
